# Changelog

## 1.0.0 (2026-04-08)

- Initial release
- Add `/speckit.refine.update` command for in-place spec refinement
- Add `/speckit.refine.propagate` command for cascading changes to plan and tasks
- Add `/speckit.refine.diff` command for read-only impact analysis
- Add `/speckit.refine.status` command for artifact sync dashboard
- Add optional hooks after specify and plan commands
- Addresses community request in issue #1191 (101+ upvotes)
